<scc:ThemedUserControl x:Class="Supremacy.Client.Views.AssetsScreen"
                       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                       xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                       xmlns:scc="http://schemas.startreksupremacy.com/xaml/presentation"
                       xmlns:s="http://schemas.startreksupremacy.com/xaml/core"
                       xmlns:sx="http://schemas.startreksupremacy.com/xaml/core/markup"
                       xmlns:v="clr-namespace:Supremacy.Client.Views"
                       mc:Ignorable="d"
                       d:DesignHeight="768"
                       d:DesignWidth="1280"
                       d:DataContext="{d:DesignInstance Type=scc:AssetsScreenPresentationModel, IsDesignTimeCreatable=True}"
                       Foreground="{DynamicResource {x:Static scc:ClientResources.DefaultTextForegroundBrushKey}}"
                       Background="#66000000"
                       UseLayoutRounding="True"
                       Margin="6,6,6,6">
    
    
    <TabControl>

        <!--Left side COMBAT_FRIENDLY_UNITS source path & items name to DataTemple in .cs  +-->
        <!--<GroupBox Header="{s:StringResource COMBAT_FRIENDLY_UNITS}"
                  Foreground="Ivory"
                  Background="Black"
                    Grid.Column="0"
                    Grid.Row="1"
                  VerticalAlignment="Top"
                  Height="620"
                  MaxHeight ="620"
                    Width="Auto"            
                    Margin="2">
            <TreeView x:Name="FriendlyAssetsTree"
                    ItemsSource="{Binding FriendlyCivs}"
                    BorderThickness="0"
                    VerticalAlignment="Top"
                    Height="541"
                    FontFamily="{DynamicResource {x:Static scc:ClientResources.DefaultFontFamilyKey}}"
                    FontSize="10pt"
                    ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                    ScrollViewer.VerticalScrollBarVisibility="Auto">
                <Border x:Name="FriendsBorder"
                        Grid.Column="0"
                        Grid.Row="1"
                        DockPanel.Dock="Top"
                        VerticalAlignment="Top"
                        HorizontalAlignment="Left"
                        Margin="0,4,4,10"
                        Width="180"
                        Height="auto"
                        BorderBrush="Gray"
                        BorderThickness="4"
                        CornerRadius="8">
                    <ListBox x:Name="SpyCivilizationsItems"  
                            IsSynchronizedWithCurrentItem="True"
                            ItemsSource="{Binding}"
                            ItemTemplate="{StaticResource FriendTreeTemplate}"
                            Grid.Column="0"
                            Grid.Row="1"     
                            BorderThickness="0" 
                            VerticalAlignment="Top"
                            FontFamily="{DynamicResource {x:Static scc:ClientResources.DefaultFontFamilyKey}}"
                            FontSize="14pt">
                    </ListBox>
                </Border>
                <StackPanel 
                            Grid.Column="0"
                            Grid.Row="1"
                            Grid.ColumnSpan="2"
                            Orientation="Horizontal"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Left">
                    <Border x:Name="ImageBorderFriend"
                                DockPanel.Dock="Left"
                                VerticalAlignment="Top"
                                Width="55"
                                Height="52"
                                BorderBrush="White"
                                BorderThickness="2"
                                CornerRadius="8">
                        <Border.Background>
                            <ImageBrush ImageSource="{Binding Path=Owner, Converter={StaticResource CivInsigniaConverter}}"
                                Stretch="UniformToFill"/>
                        </Border.Background>
                    </Border>
                    <StackPanel 
                                Grid.Column="0"
                                Grid.Row="1"
                                Grid.ColumnSpan="2"
                                Orientation="Vertical"
                                VerticalAlignment="Top"
                                HorizontalAlignment="Left">
                        <StackPanel 
                                    Grid.Column="0"
                                    Grid.Row="1"
                                    Grid.ColumnSpan="2"
                                    Margin="4,0,0,0"
                                    Orientation="Horizontal"
                                    VerticalAlignment="Top"
                                    HorizontalAlignment="Left">
                            <TextBlock  Foreground="White"
                                        FontFamily="{DynamicResource {x:Static scc:ClientResources.HeaderFontFamilyKey}}"
                                        FontSize="16pt" 
                                        Text="{s:StringResource COMBAT_FRIENDLY_FIREPOWER}"/>
                            <TextBlock Foreground="White"
                                       Margin="4,0,0,0"
                                        FontFamily="{DynamicResource {x:Static scc:ClientResources.HeaderFontFamilyKey}}"
                                        FontSize="16pt" 
                                        Text="{Binding Path=FriendlyEmpireStrength, StringFormat='#,#\\'}"/>
                        </StackPanel>
                        <StackPanel 
                                Grid.Column="0"
                                Grid.Row="1"
                                Grid.ColumnSpan="2"
                                Margin="4,0,0,0"
                                Orientation="Horizontal"
                                VerticalAlignment="Top"
                                HorizontalAlignment="Left">
                            <TextBlock Foreground="DarkOrange"
                                        FontFamily="{DynamicResource {x:Static scc:ClientResources.HeaderFontFamilyKey}}"
                                        FontSize="16pt" 
                                        Text="{s:StringResource COMBAT_OTHERS_FIREPOWER}"/>
                            <TextBlock Foreground="Red" 
                                        Margin="4,0,0,0"
                                        FontFamily="{DynamicResource {x:Static scc:ClientResources.HeaderFontFamilyKey}}"                                      
                                        FontSize="16pt" 
                                        Text="{Binding Path=AllHostileEmpireStrength, StringFormat='#,#\\'}"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
                <TreeViewItem Name="FriendlyStationItem"/>
                <TreeViewItem Name="FriendlyCombatantItems" IsExpanded="True"/>
                <TreeViewItem Name="FriendlyNonCombatantItems" IsExpanded="True"/>
                <TreeViewItem Name="FriendlyDestroyedItems" IsExpanded="True"/>
                <TreeViewItem Name="FriendlyAssimilatedItems" IsExpanded="True"/>
                <TreeViewItem Name="FriendlyEscapedItems" IsExpanded="True"/>
            </TreeView>
        </GroupBox>-->
        <!--TAB for Spyed Colonies-->
        <TabItem Header="{sx:LocalizedString Group={x:Static s:LocalizedTextGroups.AssetsScreen}, Entry=SPY_REPORT_TAB_HEADER}">
            <!--Grid just for Head line-->
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="40" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock 
                        Grid.Column="0"
                        Grid.Row="0"  
                        VerticalAlignment="Top"
                        Text="Civilization"

                        Foreground="{DynamicResource {x:Static scc:ClientResources.DefaultTextForegroundBrushKey}}"
                        FontFamily="{DynamicResource {x:Static scc:ClientResources.DefaultFontFamilyKey}}"
                        FontSize="12pt"/>
                    <TextBlock
                        Grid.Column="2" 
                        Grid.Row="0"
                        Text="{Binding Path=SpyCivName}" 
                        VerticalAlignment="Top"
                        Foreground="Crimson"
                        FontStyle="Oblique"
                        FontSize="12pt"/>
                    <TextBlock 
                        Grid.Column="0"
                        Grid.Row="0"
                        VerticalAlignment="Bottom"
                        Text="Total Empire Population"
                        Foreground="{DynamicResource {x:Static scc:ClientResources.DefaultTextForegroundBrushKey}}"
                        FontFamily="{DynamicResource {x:Static scc:ClientResources.DefaultFontFamilyKey}}"
                        FontSize="12pt"/>
                    <TextBlock 
                        Grid.Column="2" 
                        Grid.Row="0"
                        Text="{Binding Path=SpyTotalPopulation}" 
                        VerticalAlignment="Bottom"
                        Foreground="{DynamicResource {x:Static scc:ClientResources.DefaultTextForegroundBrushKey}}"
                        FontFamily="{DynamicResource {x:Static scc:ClientResources.DefaultFontFamilyKey}}"
                        FontSize="12pt"/>
                </Grid>
                <v:SpyColonyList Grid.Row="2" DataContext="{Binding Path=SpyColonies}" />
                <Button x:Name="SummaryButton"
                        Grid.Row="0"
                        VerticalAlignment="Top"
                        HorizontalAlignment="Right"
                        Width="250"
                        Height="30"
                        Style="{StaticResource LCARS_StandardButtonStyle}"
                        Content="Click for Summary"
                        FontSize="14pt"
                        FontFamily="{StaticResource {x:Static scc:ClientResources.HeaderFontFamilyKey}}"
                        Foreground="Black"
                        Margin="0,0,0,0"
                        Command="{x:Static scc:ClientCommands.ShowEndOfTurnSummary}"></Button>
            </Grid>
        </TabItem>
        <!--TAB for COLONIES-->
        <TabItem Header="{sx:LocalizedString Group={x:Static s:LocalizedTextGroups.AssetsScreen}, Entry=COLONIES_TAB_HEADER}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock 
                        Grid.Column="0" Text="Total Empire Population"
                        Foreground="{DynamicResource {x:Static scc:ClientResources.DefaultTextForegroundBrushKey}}"
                        FontFamily="{DynamicResource {x:Static scc:ClientResources.DefaultFontFamilyKey}}"
                        FontSize="12pt"/>
                    <TextBlock 
                        Grid.Column="2" Text="{Binding Path=TotalPopulation}" 
                        Foreground="{DynamicResource {x:Static scc:ClientResources.DefaultTextForegroundBrushKey}}"
                        FontFamily="{DynamicResource {x:Static scc:ClientResources.DefaultFontFamilyKey}}"
                        FontSize="12pt"/>
                </Grid>
                <v:ColonyList Grid.Row="2" DataContext="{Binding Path=Colonies}" />
            </Grid>
        </TabItem>
        <!--TAB for STATIONS-->
        <TabItem Header="{sx:LocalizedString Group={x:Static s:LocalizedTextGroups.AssetsScreen}, Entry=STATIONS_TAB_HEADER}">
                <Grid>
                <!--Grid just for Head line-->
                <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="5"/>
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock 
                        Grid.Row="1"
                        Grid.Column="0" Text="Total Empire Population"
                        Foreground="{DynamicResource {x:Static scc:ClientResources.DefaultTextForegroundBrushKey}}"
                        FontFamily="{DynamicResource {x:Static scc:ClientResources.DefaultFontFamilyKey}}"
                        FontSize="12pt"/>
                        <TextBlock 
                        Grid.Row="1"
                        Grid.Column="1" Text="{Binding Path=TotalPopulation}" 
                        Foreground="{DynamicResource {x:Static scc:ClientResources.DefaultTextForegroundBrushKey}}"
                        FontFamily="{DynamicResource {x:Static scc:ClientResources.DefaultFontFamilyKey}}"
                        FontSize="12pt"/>
                        <TextBlock 
                        Grid.Row="2"
                        Grid.Column="0" Text="Total Empire Population"
                        Foreground="{DynamicResource {x:Static scc:ClientResources.DefaultTextForegroundBrushKey}}"
                        FontFamily="{DynamicResource {x:Static scc:ClientResources.DefaultFontFamilyKey}}"
                        FontSize="12pt"/>
                        <TextBlock 
                        Grid.Row="2"
                        Grid.Column="1" Text="{Binding Path=TotalPopulation}" 
                        Foreground="{DynamicResource {x:Static scc:ClientResources.DefaultTextForegroundBrushKey}}"
                        FontFamily="{DynamicResource {x:Static scc:ClientResources.DefaultFontFamilyKey}}"
                        FontSize="12pt"/>
                        <TextBlock 
                        Grid.Row="3"
                        Grid.Column="0" Text="Total Empire Population"
                        Foreground="{DynamicResource {x:Static scc:ClientResources.DefaultTextForegroundBrushKey}}"
                        FontFamily="{DynamicResource {x:Static scc:ClientResources.DefaultFontFamilyKey}}"
                        FontSize="12pt"/>
                        <TextBlock 
                        Grid.Row="3"
                        Grid.Column="1" Text="{Binding Path=TotalPopulation}" 
                        Foreground="{DynamicResource {x:Static scc:ClientResources.DefaultTextForegroundBrushKey}}"
                        FontFamily="{DynamicResource {x:Static scc:ClientResources.DefaultFontFamilyKey}}"
                        FontSize="12pt"/>
                    </Grid>
                    
                    <!--<v:StationsList Grid.Row="2" DataContext="{Binding Path=Colonies}" />-->
                    
                </Grid>
            </TabItem>
        <!--TAB for Spy Ship Report of colony-->
        <TabItem Header="{sx:LocalizedString Group={x:Static s:LocalizedTextGroups.AssetsScreen}, Entry=SHIPS_TAB_HEADER}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="30" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock 
                        Grid.Column="0" Text="Total Empire Population"
                        Foreground="{DynamicResource {x:Static scc:ClientResources.DefaultTextForegroundBrushKey}}"
                        FontFamily="{DynamicResource {x:Static scc:ClientResources.DefaultFontFamilyKey}}"
                        FontSize="12pt"/>
                    <TextBlock 
                        Grid.Column="2" Text="{Binding Path=TotalPopulation}" 
                        Foreground="{DynamicResource {x:Static scc:ClientResources.DefaultTextForegroundBrushKey}}"
                        FontFamily="{DynamicResource {x:Static scc:ClientResources.DefaultFontFamilyKey}}"
                        FontSize="12pt"/>
                    <TextBlock 
                        Grid.Column="3" Text="Spy Ship Report to follow"
                        Foreground="{DynamicResource {x:Static scc:ClientResources.DefaultTextForegroundBrushKey}}"
                        FontFamily="{DynamicResource {x:Static scc:ClientResources.DefaultFontFamilyKey}}"
                        FontSize="12pt"/>
                </Grid>
            </Grid>
        </TabItem>
      
        <!--TAB for Economy-->
        <TabItem Header="{sx:LocalizedString Group={x:Static s:LocalizedTextGroups.AssetsScreen}, Entry=ECONOMY_TAB_HEADER}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="30" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock 
                        Grid.Column="0" Text="Total Empire Population"
                        Foreground="{DynamicResource {x:Static scc:ClientResources.DefaultTextForegroundBrushKey}}"
                        FontFamily="{DynamicResource {x:Static scc:ClientResources.DefaultFontFamilyKey}}"
                        FontSize="12pt"/>
                    <TextBlock 
                        Grid.Column="2" Text="{Binding Path=TotalPopulation}" 
                        Foreground="{DynamicResource {x:Static scc:ClientResources.DefaultTextForegroundBrushKey}}"
                        FontFamily="{DynamicResource {x:Static scc:ClientResources.DefaultFontFamilyKey}}"
                        FontSize="12pt"/>
                    <TextBlock 
                        Grid.Column="3" Text="It is the economy stupid"
                        Foreground="{DynamicResource {x:Static scc:ClientResources.DefaultTextForegroundBrushKey}}"
                        FontFamily="{DynamicResource {x:Static scc:ClientResources.DefaultFontFamilyKey}}"
                        FontSize="12pt"/>
                </Grid>
            </Grid>
        </TabItem>

    </TabControl>
</scc:ThemedUserControl>
